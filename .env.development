# Development environment variables
# Used when running `nx serve` locally

# Component showcase URL (embedded in nav-shell via iframe)
SHOWCASE_URL=http://localhost:4300

# API Gateway URL (future use)
API_GATEWAY_URL=http://localhost:3333
